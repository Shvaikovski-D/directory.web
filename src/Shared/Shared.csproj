<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>directory.web.Shared</RootNamespace>
    <AssemblyName>directory.web.Shared</AssemblyName>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
